[package]
name = "stool"
version = "0.2.0"
edition = "2024"
license = "MIT"
readme = "README.md"
description = "Smart Git multi-repo management CLI."

[[bin]]
name = "st"
path = "src/main.rs"

[dependencies]
clap = { version = "4.5.4", features = ["derive"]}
regex = "1.10"
